import React from 'react';

const Home = ({ feature }) => {
    
    console.log(feature);
    return (
        <div>
            <h1>This is home page.</h1>
        </div>
    );
};

export default Home;